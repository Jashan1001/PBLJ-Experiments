# Database Configuration
# Change these settings according to your MySQL setup

# Part B: Hibernate Configuration
db.student.url=jdbc:mysql://localhost:3306/student_db?useSSL=false&serverTimezone=UTC
db.student.username=root
db.student.password=password

# Part C: Banking Application Configuration
db.banking.url=jdbc:mysql://localhost:3306/banking_db?useSSL=false&serverTimezone=UTC
db.banking.username=root
db.banking.password=password

# Hibernate Settings
hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
hibernate.show_sql=true
hibernate.format_sql=true
hibernate.hbm2ddl.auto=update

# Connection Pool Settings
db.pool.initialSize=5
db.pool.maxTotal=10

